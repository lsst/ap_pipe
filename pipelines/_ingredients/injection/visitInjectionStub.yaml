description: Single-frame-level source injection into visit dataset types.
tasks:
  inject_visit:
    class: lsst.source.injection.inject_visit.VisitInjectTask
    config:
      external_psf: false
      external_photo_calib: false
      external_wcs: false
